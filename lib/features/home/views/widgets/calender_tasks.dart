import 'package:flutter/material.dart';

class ClanederTasks extends StatelessWidget {
  const ClanederTasks({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
